import React from "react";
import ReactDOM from "react-dom/client";
import JiraChronicles from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JiraChronicles />
  </React.StrictMode>,
);
